import { makeStyles } from "@material-ui/core/styles";

export const lockNFTModalStyles = makeStyles(theme => ({
  root: {
    maxWidth: "700px !important",
  },
  divider: {
    marginLeft: -32,
    marginRight: -32,
  },
}));
