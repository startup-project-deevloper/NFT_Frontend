import { makeStyles } from "@material-ui/core/styles";

export const transactionStyles = makeStyles(theme => ({
  root: {
    width: '100%',
  },
  title: {
    fontWeight: 800,
    fontSize: 22,
    lineHeight: '130%',
    color: '#FFFFFF',
    marginBottom: 25,
  }
}));
