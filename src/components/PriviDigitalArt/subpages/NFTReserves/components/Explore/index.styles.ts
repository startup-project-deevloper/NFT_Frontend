import { makeStyles } from "@material-ui/core/styles";
import { Color } from "shared/ui-kit";

export const exploreOptionStyles = makeStyles(theme => ({
    content : {
        padding: '35px',
        width:'100%'
    }
}));