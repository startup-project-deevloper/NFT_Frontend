import { makeStyles } from "@material-ui/core";

export const ChangeToSyntheticModalStyles = makeStyles(theme => ({
  root: {
    maxWidth: '700px !important',
  },
  divider: {
    marginLeft: -32,
    marginRight: -32,
  },
}));
