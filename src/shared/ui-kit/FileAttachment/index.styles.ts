import { makeStyles } from "@material-ui/core/styles";

export const useFileAttachmentStyles = makeStyles(() => ({
  attachment: {
    marginLeft: 10,
    marginRight: 10,
    cursor: "pointer",
    display: "flex",
  },
}));
